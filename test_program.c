#include <stdio.h>

int main() {
    int i, N = 100;
    int A[100], c, d, e, f;
    for(i = 0; i < N; i++) {
        int t1, t2, t3, t4, *t;
        *c = *d + 5;
        *e = *f * 2;
        if (A[i] == 1) {
            d = d + 1;
            f = f - 1;
        } else {
            t = e;
            e = c + 2;
            c = t - 3;
        }
    }
    return 0;
} 